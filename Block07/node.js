console.log ("Lock Combination")
let text =" You have received this message because you have been chosen to open an important vault. Here is the secret combination:"
//Variables for the combination
const ten = 5+5;
const forty = 30+10;
const thirtynine = 38+1;
//Pop up Alert
alert(
     `The combination for the lock is ${ten} -${forty} - ${thirtynine}`
    );